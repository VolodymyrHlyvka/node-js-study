extends layout/layout.pug
block content 
    h1.title Shop from node pug js
    h2 List of all products

    main 
        if products.length >0
            ul 
                each product in products
                    li #{product.name}
        else 
            h3 No products